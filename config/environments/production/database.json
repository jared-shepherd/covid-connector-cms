{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "uri": "${process.env.PROD_DB_URI}",
        "database": "${process.env.PROD_DB_NAME}"
      },
      "options": {
        "ssl": "true"
      }
    }
  }
}
