{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "uri": "${process.env.STAGE_DB_URI}",
        "database": "${process.env.STAGE_DB_NAME}"
      },
      "options": {
        "ssl": "true"
      }
    }
  }
}
