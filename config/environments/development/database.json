{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "database": "covid-connector-cms",
        "uri": "${process.env.DB_URI || ''}"
      },
      "options": {
        "ssl": "true"
      }
    }
  }
}
