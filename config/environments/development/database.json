{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "uri": "${process.env.DEV_DB_URI}",
        "database": "${process.env.DEV_DB_NAME}"
      },
      "options": {
        "ssl": "true"
      }
    }
  }
}
